defmodule AshReports.MinimalCompilationTest do
  @moduledoc """
  Minimal test to ensure basic DSL compilation works.
  """

  use ExUnit.Case

  test "basic DSL compilation" do
    # This test just ensures the basic DSL structure compiles
    assert true
  end
end
